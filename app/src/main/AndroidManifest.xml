<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pay2Share"
        tools:targetApi="31">

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Pay2Share.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Pay2Share.NoActionBar">
        </activity>

        <activity
            android:name=".ui.register.RegisterActivity"
            android:exported="true"
            android:theme="@style/Theme.Pay2Share.NoActionBar">
        </activity>

        <activity
            android:name=".ui.group.CreateGroupActivity"
            android:exported="true"
            android:theme="@style/Theme.Pay2Share.NoActionBar">
        </activity>

        <activity
            android:name=".ui.group.GroupDetailActivity"
            android:exported="true"
            android:theme="@style/Theme.Pay2Share.NoActionBar">
        </activity>

        <!-- Agregar la declaración de UserDetailActivity -->
        <activity
            android:name=".ui.group.UserDetailActivity"
            android:exported="true"
            android:theme="@style/Theme.Pay2Share.NoActionBar">
        </activity>

        <activity android:name=".ui.group.ParticipantDetailActivity" />



    </application>

</manifest>